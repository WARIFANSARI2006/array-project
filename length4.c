#include<stdio.h>
#include<conio.h>
#include<string.h>
void main(){

	char str1[3];
	int length;
	printf("enter the string\n");
	gets(str1);
	length=strlen(str1);
	printf("length=%d",length);
	getch();






}